// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Identifier } from "../../hl7-fhir-r4-core/Identifier";
import type { Organization } from "../../hl7-fhir-r4-core/Organization";
import type { Period } from "../../hl7-fhir-r4-core/Period";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

// CanonicalURL: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Organization
export type UKCoreOrganization_Identifier_OdsOrganisationCodeSliceInput = Omit<Identifier, "system">;
export type UKCoreOrganization_Identifier_OdsSiteCodeSliceInput = Omit<Identifier, "system">;

import { applySliceMatch, matchesSlice, extractSliceSimplified } from "../../profile-helpers";

export class UKCoreOrganizationProfile {
    private resource: Organization

    constructor (resource: Organization) {
        this.resource = resource
    }

    toResource () : Organization {
        return this.resource
    }

    public setMainLocation (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "https://fhir.hl7.org.uk/StructureDefinition/Extension-UKCore-MainLocation", valueReference: value })
        return this
    }

    public setOrganizationPeriod (value: Period): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/organization-period", valuePeriod: value })
        return this
    }

    public setOdsOrganisationCode (input?: UKCoreOrganization_Identifier_OdsOrganisationCodeSliceInput): this {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-organization-code"} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as Identifier
        const list = (this.resource.identifier ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setOdsSiteCode (input?: UKCoreOrganization_Identifier_OdsSiteCodeSliceInput): this {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-site-code"} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as Identifier
        const list = (this.resource.identifier ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getMainLocation (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "https://fhir.hl7.org.uk/StructureDefinition/Extension-UKCore-MainLocation")
        return ext?.valueReference
    }

    public getMainLocationExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "https://fhir.hl7.org.uk/StructureDefinition/Extension-UKCore-MainLocation")
        return ext
    }

    public getOrganizationPeriod (): Period | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/organization-period")
        return ext?.valuePeriod
    }

    public getOrganizationPeriodExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/organization-period")
        return ext
    }

    public getOdsOrganisationCode (): UKCoreOrganization_Identifier_OdsOrganisationCodeSliceInput | undefined {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-organization-code"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["system"]) as UKCoreOrganization_Identifier_OdsOrganisationCodeSliceInput
    }

    public getOdsOrganisationCodeRaw (): Identifier | undefined {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-organization-code"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getOdsSiteCode (): UKCoreOrganization_Identifier_OdsSiteCodeSliceInput | undefined {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-site-code"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["system"]) as UKCoreOrganization_Identifier_OdsSiteCodeSliceInput
    }

    public getOdsSiteCodeRaw (): Identifier | undefined {
        const match = {"system":"https://fhir.nhs.uk/Id/ods-site-code"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

}

