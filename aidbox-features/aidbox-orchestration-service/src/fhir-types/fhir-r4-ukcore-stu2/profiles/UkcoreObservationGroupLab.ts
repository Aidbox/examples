// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Observation } from "../../hl7-fhir-r4-core/Observation";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

// CanonicalURL: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Observation-Group-Lab
export interface UKCoreObservationGroupLab extends Observation {
    category: CodeableConcept[];
    subject?: Reference<"Patient">;
}

export type UKCoreObservationGroupLab_TriggeredByR5Input = {
    observation: Reference;
    type: string;
    reason?: string;
}

export type UKCoreObservationGroupLab_Category_LaboratorySliceInput = Omit<CodeableConcept, "coding">;

import { applySliceMatch, matchesSlice, extractComplexExtension, extractSliceSimplified } from "../../profile-helpers";

export class UKCoreObservationGroupLabProfile {
    private resource: Observation

    constructor (resource: Observation) {
        this.resource = resource
    }

    toResource () : Observation {
        return this.resource
    }

    toProfile () : UKCoreObservationGroupLab {
        return this.resource as UKCoreObservationGroupLab
    }

    public setTriggeredByR5 (input: UKCoreObservationGroupLab_TriggeredByR5Input): this {
        const subExtensions: Extension[] = []
        if (input.observation !== undefined) {
            subExtensions.push({ url: "observation", valueReference: input.observation })
        }
        if (input.type !== undefined) {
            subExtensions.push({ url: "type", valueCode: input.type })
        }
        if (input.reason !== undefined) {
            subExtensions.push({ url: "reason", valueString: input.reason })
        }
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.triggeredBy", extension: subExtensions })
        return this
    }

    public setBodyStructureR5 (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.bodyStructure", valueReference: value })
        return this
    }

    public setLaboratory (input?: UKCoreObservationGroupLab_Category_LaboratorySliceInput): this {
        const match = {"coding":{"code":"laboratory"}} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as CodeableConcept
        const list = (this.resource.category ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getTriggeredByR5 (): UKCoreObservationGroupLab_TriggeredByR5Input | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.triggeredBy")
        if (!ext) return undefined
        const config = [{ name: "observation", valueField: "valueReference", isArray: false }, { name: "type", valueField: "valueCode", isArray: false }, { name: "reason", valueField: "valueString", isArray: false }]
        return extractComplexExtension(ext as unknown as { extension?: Array<{ url?: string; [key: string]: unknown }> }, config) as UKCoreObservationGroupLab_TriggeredByR5Input
    }

    public getTriggeredByR5Extension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.triggeredBy")
        return ext
    }

    public getBodyStructureR5 (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.bodyStructure")
        return ext?.valueReference
    }

    public getBodyStructureR5Extension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/5.0/StructureDefinition/extension-Observation.bodyStructure")
        return ext
    }

    public getLaboratory (): UKCoreObservationGroupLab_Category_LaboratorySliceInput | undefined {
        const match = {"coding":{"code":"laboratory"}} as Record<string, unknown>
        const list = this.resource.category
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["coding"]) as UKCoreObservationGroupLab_Category_LaboratorySliceInput
    }

    public getLaboratoryRaw (): CodeableConcept | undefined {
        const match = {"coding":{"code":"laboratory"}} as Record<string, unknown>
        const list = this.resource.category
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

}

